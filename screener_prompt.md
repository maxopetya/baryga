# Утренний скринер MOEX — инструкция

Задача: за 5-10 минут собрать компактный брифинг на открытие торгов, используя
- `output/daily_news_YYYY-MM-DD.md` — новости за 16 ч
- `output/preopen_YYYY-MM-DD.md` — pre-open snapshot с overnight-факторами и модельным прогнозом
- MOEX ISS через скилл `moex-iss` — для уточнений

## Формат брифинга (канон)

Пишешь в `output/daily_briefing_YYYY-MM-DD.md`, дальше это уходит в Telegram.

```
<b>📊 DD.MM день · MOEX через N мин</b>

Настроение <позитивное|нейтральное|негативное> · США ±X% · золото ±X% · нефть ±X%

<b>Топ-15 вверх (≤4 на сектор)</b>
<pre>Короткое имя   (TICKER)  +X.X%  — драйвер (уверенность)
... ровно 15 строк с выравненными колонками ...</pre>

<b>Осторожно</b>
Короткое имя (TICKER)   −X…−Y% — что случилось (уверенность)
... 1-5 строк, только если есть реальные негативы ...

<b>Отдельно</b>
Одна-две фразы про новость, которая заслуживает внимания и даёт сильную уверенность. Ссылка на источник. Если таких новостей нет — секцию не пишем вообще.

% — базовое ожидание модели, не таргет.
Не является инвестиционной рекомендацией.
```

**Выравнивание через `<pre>`.** Внутри блока — моноширинный шрифт, пробелы сохраняются. Форматирование колонок:
- Имя: padding до 15 символов
- `(TICKER)`: padding до 8 символов (все MOEX-тикеры ≤ 5 букв → `(SELG)  ` = 8 символов)
- `+X.X%`: с пробелом перед знаком для отрицательных
- Двойной пробел + `— драйвер (уверенность)`

## Правила заполнения

**Настроение** — качественная оценка:
- 🟢 позитивное — предсказанный IMOEX > +0.3%
- 🟡 нейтральное — |IMOEX| ≤ 0.3%
- 🔴 негативное — IMOEX < −0.3%

**Топ-15 вверх** — сортировка по ожидаемому % из `finance.prognosis --overnight`, с ограничением **не более 4 бумаг на сектор**. Диверсификация важнее того, чтобы список забивался одной темой (например, 8 нефтяников из 15 в бычий по нефти день). Ровно 15 строк.

**Названия компаний** — короткая узнаваемая форма без «ПАО»/«АО»/«МКПАО»/«ОАО»:
- Татнефть, Роснефть, Лукойл, Газпром, Новатэк
- Норникель, Полюс, ММК (не Магнитогорск.мет.комб)
- Т-Банк (не Т-Технологии МКПАО), Сбер (не Сбербанк)
- Мечел преф (когда MTLRP), Мечел (когда MTLR)
- Ozon, Yandex, ВК, Астра
- Россети Центр, Юнипро, РусГидро
Если есть только длинное юридическое имя из справочника — сократи здравым смыслом.

**Никаких эмодзи в строках тикеров** — только чистый текст. Эмодзи в заголовке (`📊`) допустимо.

**Драйвер** — 3-5 слов, чем ты объясняешь модельный сигнал. Примеры:
- «США + нефтянка» — SPX overnight сильный + сектор MOEXOG
- «золото» — Gold overnight сильный + сектор MOEXMM
- «слабый рубль» — USDRUB overnight выше, экспортёры выигрывают
- «IT-сектор» — MOEXIT прогноз выше рынка
- Если сигнал слабый: «общерыночный»

**Уверенность в скобках** — это уверенность в **направлении** (пойдёт вверх/вниз), НЕ в амплитуде. Формула:
```
ratio = |ожидаемый %| / vol_daily_%
```
где vol_daily — историческая дневная волатильность бумаги (из ticker_vol).

- **высокая**: ratio ≥ 0.7 — прогнозируемое движение сравнимо или больше типичного дневного шума, направление явно
- **средняя**: 0.4 ≤ ratio < 0.7 — сигнал внутри шума, но выделяется
- **низкая**: ratio < 0.4 — сигнал в пределах шума, направление легко может развернуться

**Исключения (override → высокая):**
- Жёсткий корпоративный триггер: дефолт, санкции, buyback объявлен, дивиденды рекомендованы, отчёт выпущен — направление реакции почти детерминировано, даже если модель не в топе.

**Важно:** уверенность НЕ означает «прогноз в +1.5% точно», это «направление вверх — почти наверняка при базовом фоне». Реальная амплитуда может быть в 2-5 раз больше или меньше — R² модели на амплитуду низкий (8-13% для факторов индекса).

## Что НЕ включать

- **Ссылки на источники внутри Telegram** — они делают сообщение раздутым; ссылки живут в файле `preopen_*.md`, куда пользователь может зайти
- **Детали модели** (β, R², vol, названия факторов) — это ушло в отдельные бэктест-отчёты
- **«Что смотрю руками», «Watch», «Наблюдение»** — секции, полезные аналитику, но нет пользователю
- **Дисклеймер на 3 абзаца** — одна строчка в конце достаточно
- **Пояснение звёздочек/градаций** — слово-уверенность само по себе понятно

## Дисциплина

1. **Не выдумывай цифры**. Если у бумаги нет модельного прогноза — не включай.
2. **Не давай узких таргетов** («до 145 руб») — % это ожидание, а не таргет.
3. **Не расширяй «Осторожно»** без явного триггера. Слабый негативный сигнал модели ≠ «осторожно», это «низкий приоритет».
4. **Проверяй, чтобы 10 строк вверх были действительно топ-10** из `finance.prognosis --overnight`, не «топ по моему вкусу».
5. **HTML-теги, не Markdown**. Telegram HTML предсказуемее, `<b>`/`<i>` работают.

## После брифинга

Скажи пользователю:
- Одна фраза: настроение дня + топ-3 по силе
- Спроси, нужно ли углубить конкретный тикер (тогда идёшь в MOEX ISS через `moex-iss` за историей движения, объёмами, ищешь корпоративные новости)
