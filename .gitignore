.env
.venv/
__pycache__/
*.pyc
# data/finance.db остаётся в репозитории — там калибровка + регрессия + история новостей
data/*.db-*
output/*.log
*.log

# .env.example остаётся; сюда попадает только настоящий .env
